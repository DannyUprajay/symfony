<div>
    {% if app.user %}
{#        je suis connecté#}
            <a href="{{ path('app_logout') }}"> Se déconnecter</a>

            {% if is_granted('ROLE_ADMIN') %}
            <a href="{{ path('app_admin') }}">Administration</a>
            {% endif %}
    {% else %}
{# je suis pas connecté#}
            <a href="{{ path('app_login') }}">Se connecter</a>
            <a href="{{ path('app_register') }}">S'enregistre
                r</a>
    {% endif %}
</div>